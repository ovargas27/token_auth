This is an example app about How to use token authentication with devise

## Configuration

Modify `remote_host` variable to you app with devise authentication

```js
  var remote_host = "http://localhost:3000"
```

